name: clustal_env
channels:
    - conda-forge
    - bioconda
    - nodefaults
dependencies:
    - python
    - numpy
    - pandas
    - biopython
    # Linters and formateurs
    - pycodestyle
    - pydocstyle
    - pylint
    - black
    - ruff
